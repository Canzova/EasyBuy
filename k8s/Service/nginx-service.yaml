apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-service-lable-app
spec:
  selector:
    app: nginx-pod-lable-name
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 80
  type: LoadBalancer
