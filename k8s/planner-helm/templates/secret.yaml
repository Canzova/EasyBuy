apiVersion: v1
kind: Secret
metadata:
  name: {{ include "planner.secretName" . }}
  labels:
    {{- include "planner.labels" . | nindent 4 }}
type: Opaque
data:
  DB_PASSWORD: {{ .Values.secret.dbPassword | b64enc | quote }}
  MYSQL_PASSWORD: {{ .Values.secret.mysqlPassword | b64enc | quote }}
